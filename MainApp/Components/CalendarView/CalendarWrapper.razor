<table class="app-calendar table table-bordered border-@(Theme.Light.ToString().ToLower())-subtle align-top mb-0 @(CalendarHasSubheader ? "caption-top app-calendar-height-with-subheader" : "h-100")">
    @CalendarSubHeaderView
    @CalendarHeaderView
    @CalendarBodyView
</table>
